
def rotatingProxies():
    # create 30 different proxies
    # 28 batches , each get one proxy and we make some back ups 
    # if one fails / gets blocked the backups can intervine

    # fetch an IP adress that is not in the proxy list ( make it run on mem not in DB)
    
    # start the collection 

    # if the IP fails we get back here and get a new (or we fetch every singel tow IPs 
    # maybe each thread can swith between them or some )
